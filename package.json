{
  "name": "libheif-wasm",
  "version": "0.1.0",
  "description": "A WebAssembly implementation of heif decoder",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build-wasm-debug": "./tools/build_wasm.sh debug",
    "build-wasm-release": "./tools/build_wasm.sh release",
    "build": "tsc && cp src/libheif.js dist && cp src/libheif.wasm dist",
    "test": "jest",
    "clean": "rm -rf build dist",
    "prepublishOnly" : "npm run clean && npm run build-wasm-release && npm run build && npm test"
  },
  "keywords": [
    "heif",
    "heic",
    "avif",
    "wasm"
  ],
  "author": "seven332",
  "license": "MIT",
  "devDependencies": {
    "@jest/globals": "^29.6.1",
    "jest": "^29.6.1",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
